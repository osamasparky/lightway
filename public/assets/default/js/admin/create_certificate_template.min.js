/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!***********************************************************!*\
  !*** ./resources/js/admin/create_certificate_template.js ***!
  \***********************************************************/
(function () {
    "use strict";

    const defaultPositions = {
        title: { top: '275px', left: '165px', width: '600px', textAlign: 'center' },
        student_name: { top: '315px', left: '165px', width: '600px', textAlign: 'center' },
        subtitle: { top: '360px', left: '165px', width: '600px', textAlign: 'center' },
        body: { top: '395px', left: '165px', width: '600px', textAlign: 'center' },
        date: { top: '80px', left: '80px' },
        platform_name: { top: '50px', left: '80px' },
        hint: { top: '50px', left: '700px' },
        stamp: { top: '440px', left: '100px' },
        platform_signature: { top: '440px', left: '230px' },
        instructor_signature: { top: '440px', left: '590px' },
        qr_code: { top: '430px', left: '730px' },
        course_name: { top: '360px', left: '165px', width: '600px', textAlign: 'center' },
        instructor_name: { top: '440px', left: '590px' },
        user_certificate_additional: { top: '420px', left: '165px', width: '600px', textAlign: 'center' }
    };

    function syncContentToForm() {
        $('.js-template-contents').val($('#certificateTemplateContainer').html());
    }

    function applyDraggable() {
        $(".draggable-element").draggable({
            containment: ".certificate-template-container",
            stop: function (event, ui) {
                var $this = $(this);
                $this.css({
                    top: ui.position.top + 'px',
                    left: ui.position.left + 'px',
                    position: 'absolute'
                });
                syncContentToForm();
            }
        });
    }

    $(document).ready(function () {
        applyDraggable();
        syncContentToForm();
    });

    function removeDragElement(name) {
        $('.certificate-template-container').find('.draggable-element[data-name="' + name + '"]').remove();
    }

    function makeDragElement($accordion, name) {
        let content = $accordion.find('.js-element-content').val();
        const fontSize = $accordion.find('.js-element-font_size').val();
        const fontColor = $accordion.find('.js-element-font_color').val();
        const fontBold = $accordion.find('.js-element-font_weight_bold').is(":checked");
        const textCenter = $accordion.find('.js-element-text_center').is(":checked");
        const textRight = $accordion.find('.js-element-text_right').is(":checked");

        let $element = $(`.draggable-element[data-name="${name}"]`);
        let isNew = false;
        if ($element.length < 1) {
            $element = $(`<div class="draggable-element" data-name="${name}"></div>`);
            $('.certificate-template-container').append($element);
            isNew = true;
        }

        const def = defaultPositions[name] || { top: '100px', left: '100px' };
        const textAlign = textCenter ? 'center' : (textRight ? 'right' : (def.textAlign || 'inherit'));
        const fontWeight = fontBold ? 'bold' : 'inherit';

        let styles = "position: absolute !important;";

        let top = $element.css("top");
        let left = $element.css("left");

        let topNum = parseFloat(top);
        let leftNum = parseFloat(left);

        if (isNew || isNaN(topNum) || isNaN(leftNum) || (topNum <= 0 && leftNum >= 900)) {
            top = def.top;
            left = def.left;
        } else {
            top = topNum + 'px';
            left = leftNum + 'px';
        }

        if (name === "platform_signature" || name === "stamp") {
            const $image = $accordion.find('.js-element-image');
            let prefix = $image.attr("data-prefix") || '';
            prefix = prefix.replace(/[\\\/]+$/, '');
            let src = ($image.val() || '').trim();
            if (src && !src.startsWith('http://') && !src.startsWith('https://') && !src.startsWith('data:')) {
                if (!src.startsWith('/')) src = '/' + src;
                src = prefix + src;
            }

            content = `<img src="${src}" style="max-height: 100%; max-width: 100%;"/>`;
            styles += `display: flex; align-items: center; justify-content: center;`;
        }

        $element.html(content);

        styles += `top: ${top};`;
        styles += `left: ${left};`;
        if (def.width && (isNew || !$element[0].style.width)) {
            styles += `width: ${def.width};`;
        }
        if (fontSize && !isNaN(parseFloat(fontSize))) {
            styles += `font-size: ${parseFloat(fontSize)}px;`;
        } else if (fontSize) {
            styles += `font-size: ${fontSize};`;
        }
        if (fontColor) {
            styles += `color: ${fontColor};`;
        }
        styles += `text-align: ${textAlign};`;
        styles += `font-weight: ${fontWeight};`;

        let extraStyles = $accordion.find('.js-element-styles').val();
        styles = styles + (extraStyles ? extraStyles : '');

        $element.css('cssText', styles);

        if (name === "qr_code" || name === "platform_signature" || name === "stamp") {
            const size = $accordion.find('.js-element-image_size option:checked').val() || $accordion.find('.js-element-image_size').val();
            if (size) {
                const sizePx = isNaN(parseFloat(size)) ? size : parseFloat(size) + 'px';
                $element.css('width', sizePx);
                $element.css('height', sizePx);
            }
        }

        applyDraggable();
    }

    function afterChange($accordion, checked) {
        const elementName = $accordion.attr('data-element');
        const $statusBadge = $accordion.find('.js-status-element');

        if (checked) {
            makeDragElement($accordion, elementName);
            $statusBadge.removeClass('d-none');
        } else {
            removeDragElement(elementName);
            $statusBadge.addClass('d-none');
        }

        syncContentToForm();
    }

    $('body').on('change', '.js-element-enable', function () {
        const checked = this.checked;
        const $accordion = $(this).closest('.accordion-row');
        afterChange($accordion, checked);
    });

    $('body').on('keyup change', '.js-changeable-element-input', function () {
        const $accordion = $(this).closest('.accordion-row');
        const checked = $accordion.find('.js-element-enable').is(":checked");
        afterChange($accordion, checked);
    });

    $('body').on('change', '.js-certificate-image', function (e) {
        e.preventDefault();
        let prefix = $(this).attr('data-prefix') || '';
        prefix = prefix.replace(/[\\\/]+$/, '');
        let val = ($(this).val() || '').trim().replace(/[\\\/]+$/, '');
        $(this).val(val);

        let path = val;
        if (path && !path.startsWith('http://') && !path.startsWith('https://') && !path.startsWith('data:')) {
            if (!path.startsWith('/')) path = '/' + path;
            path = prefix + path;
        }

        if (path) {
            $('.certificate-template-container').css('background-image', "url('" + path + "')");
        }
        syncContentToForm();
    });

})(jQuery);
/******/ })()
;